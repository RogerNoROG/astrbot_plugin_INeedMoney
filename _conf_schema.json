{
  "alert_message_template": {
    "type": "text",
    "description": "余额提醒模板",
    "hint": "这是交给当前对话人格改写的预置模板，支持 {account_name}、{balance}、{threshold}、{unit} 这些占位符。",
    "default": "[余额告警]\n{account_name} 当前余额为 {balance} {unit}，已低于告警阈值 {threshold} {unit}。\n请及时充值，避免服务中断。"
  },
  "recovery_message_template": {
    "type": "text",
    "description": "余额恢复感谢模板",
    "hint": "余额从低于阈值恢复到阈值以上时发送，并由当前对话人格改写。支持 {account_name}、{balance}、{threshold}、{unit} 占位符。",
    "default": "[余额恢复]\n{account_name} 当前余额已恢复至 {balance} {unit}，高于告警阈值 {threshold} {unit}。\n感谢您的支持！"
  },
  "alert_temperature": {
    "type": "float",
    "description": "告警 AI 温度",
    "hint": "用于控制告警文案的变化程度，范围 0 到 2；数值越高，表达越灵活。",
    "default": 1.0
  },
  "enabled": {
    "type": "bool",
    "description": "启用余额监控",
    "hint": "关闭后不会请求余额接口或主动发送提醒。",
    "default": false
  },
  "platform": {
    "type": "string",
    "description": "AI 平台",
    "hint": "普通用户选择 DeepSeek；只有接口格式不同的高级用户才选择自定义接口。",
    "options": ["DeepSeek", "自定义接口"],
    "default": "DeepSeek"
  },
  "api_key": {
    "type": "string",
    "description": "DeepSeek API Key",
    "hint": "粘贴 DeepSeek 平台生成的 API Key，例如 sk-...；无需填写 Bearer。此项仅遮罩 WebUI 显示，不会加密配置文件。",
    "secret": true,
    "default": ""
  },
  "custom_api": {
    "type": "object",
    "description": "高级：自定义余额接口",
    "hint": "仅当“AI 平台”选择“自定义接口”时填写。DeepSeek 用户无需展开或配置此表单。接口必须返回 JSON，余额字段由“余额 JSON 路径”指定。",
    "items": {
      "account_name": {
        "type": "string",
        "description": "账户名称",
        "hint": "用于余额查询结果和充值提醒。",
        "default": "自定义 AI API"
      },
      "balance_api_url": {
        "type": "string",
        "description": "余额 API 地址",
        "hint": "填写完整的 http 或 https 地址。",
        "default": ""
      },
      "balance_api_method": {
        "type": "string",
        "description": "请求方法",
        "options": ["GET", "POST"],
        "default": "GET"
      },
      "api_token": {
        "type": "string",
        "description": "接口令牌",
        "hint": "填写请求头中的完整令牌值，例如 Bearer sk-...；不需要令牌时留空。",
        "secret": true,
        "default": ""
      },
      "token_header_name": {
        "type": "string",
        "description": "令牌请求头名称",
        "hint": "通常是 Authorization。",
        "default": "Authorization"
      },
      "custom_headers_json": {
        "type": "string",
        "description": "额外请求头 JSON",
        "hint": "可选。必须是字符串键和值组成的 JSON 对象，例如 {\"X-Account-ID\": \"123\"}。不要在此填写令牌。",
        "default": "{}"
      },
      "balance_api_body_json": {
        "type": "string",
        "description": "POST 请求体 JSON",
        "hint": "仅 POST 请求使用；留空表示不发送 JSON 请求体。",
        "default": ""
      },
      "balance_json_path": {
        "type": "string",
        "description": "余额 JSON 路径",
        "hint": "指定响应中的剩余余额字段。支持点号和数组索引，例如 data.balance、data.accounts[0].remaining 或 $.data.balance。",
        "default": "data.balance"
      },
      "balance_scale": {
        "type": "float",
        "description": "余额换算系数",
        "hint": "最终余额 = 响应字段数值 x 此系数。接口已直接返回余额时保持 1。",
        "default": 1.0
      },
      "balance_unit": {
        "type": "string",
        "description": "余额单位",
        "hint": "用于显示余额和告警阈值，例如 USD、CNY。",
        "default": "USD"
      }
    },
    "default": {}
  },
  "low_balance_threshold": {
    "type": "float",
    "description": "低余额告警阈值",
    "hint": "单位与所选平台的余额单位一致。余额低于该数值时发送充值提醒。",
    "default": 5.0
  },
  "poll_interval_minutes": {
    "type": "int",
    "description": "轮询间隔（分钟）",
    "hint": "至少为 1 分钟。配置修改后下一轮查询生效。",
    "default": 10
  },
  "alert_cooldown_minutes": {
    "type": "int",
    "description": "同一低余额状态的重复提醒间隔（分钟）",
    "hint": "余额恢复到阈值及以上后，下次再次低于阈值会立即提醒。设为 0 时每次轮询都提醒。",
    "default": 360
  },
  "notification_sessions": {
    "type": "list",
    "description": "允许接收提醒的会话列表",
    "hint": "填入 unified_msg_origin。管理员在目标群聊或私聊发送 /余额监控会话 可获取。只会向此列表中的会话主动推送。",
    "default": []
  },
  "receipt_code_image": {
    "type": "file",
    "description": "收款码图片",
    "hint": "低余额提醒中附带的收款码。最多使用第一个上传的图片。",
    "file_types": ["png", "jpg", "jpeg", "webp"],
    "default": []
  }
}
